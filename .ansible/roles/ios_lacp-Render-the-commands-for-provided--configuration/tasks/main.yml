---
# tasks file forios_lacp
# From file: ios_lacp.py
- name: Render the commands for provided  configuration

  cisco.ios.ios_lacp:
    config:
      system:
        priority: 123
    state: rendered
