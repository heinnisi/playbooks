---
# tasks file fornetwork_device
# From file: network_device.py
- name: get_device_by_id

  cisco.dnac.network_device:
    state: query  # required
    id: SomeValue  # string, required
  register: nm_get_device_by_id
